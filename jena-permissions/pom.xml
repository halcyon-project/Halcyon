<?xml version="1.0" encoding="UTF-8"?>

<!--
   Licensed to the Apache Software Foundation (ASF) under one or more
   contributor license agreements.  See the NOTICE file distributed with
   this work for additional information regarding copyright ownership.
   The ASF licenses this file to You under the Apache License, Version 2.0
   (the "License"); you may not use this file except in compliance with
   the License.  You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.
-->

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <artifactId>jena-permissions</artifactId>

  <name>Jena Permissions (Halcyon)</name>
  <description>Security Permissions wrapper around the Jena RDF implementation.
    Fork of org.apache.jena:jena-permissions at tag jena-5.6.0, adopted and
    maintained by the Halcyon project after the module's deprecation and removal
    in Jena 6.x. Java package namespaces are unchanged
    (org.apache.jena.permissions); the code remains Apache-2.0 licensed — see
    the license headers and the upstream NOTICE.</description>
  <url>https://jena.apache.org/documentation/permissions</url>
  <inceptionYear>2012</inceptionYear>

  <parent>
    <groupId>com.ebremer.HalcyonProject</groupId>
    <artifactId>HalcyonProject</artifactId>
    <version>0.9.0</version>
  </parent>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <automatic.module.name>org.apache.jena.permissions</automatic.module.name>
    <argLine />
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>3.12.0</version>
        <configuration>
          <doclint>none</doclint>
          <tags>
            <tag>
              <name>sec.graph</name>
              <placement>a</placement>
              <head>Required graph permissions:</head>
            </tag>
            <tag>
              <name>sec.triple</name>
              <placement>a</placement>
              <head>Required triple permissions:</head>
            </tag>
          </tags>
          <overview>${basedir}/src/main/overview.html</overview>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.5.0</version>
        <configuration>
          <archive>
            <manifestEntries>
              <Automatic-Module-Name>${automatic.module.name}</Automatic-Module-Name>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>

      <!-- Tests are the upstream JUnit 4 suite run on the JUnit 5 vintage
           engine; mockito is attached as a java agent (the dependency plugin's
           properties goal supplies ${org.mockito:mockito-core:jar}). -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.4</version>
        <configuration>
          <!-- The agent path is quoted: on Windows the local repository can
               live under a path with spaces, which unquoted breaks JVM
               argument parsing. -->
          <argLine>@{argLine} "-javaagent:${org.mockito:mockito-core:jar}" -Xshare:off</argLine>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>${maven-dependency-plugin.version}</version>
        <executions>
          <execution>
            <goals>
              <goal>properties</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <dependencies>

    <dependency>
      <groupId>org.apache.jena</groupId>
      <artifactId>jena-arq</artifactId>
      <version>${jena.version}</version>
    </dependency>

    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-collections4</artifactId>
      <version>4.5.0</version>
    </dependency>
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-lang3</artifactId>
    </dependency>

    <!-- Test dependencies. The upstream TDB1-backed tests were removed:
         they exercised non-transactional TDB graph access, a mode that no
         longer exists (TDB1 is gone in Jena 6.x and TDB2 requires
         transactions); the same graph contracts run against the in-memory
         graph in SecuredGraphContractTests. -->
    <dependency>
      <groupId>org.apache.jena</groupId>
      <artifactId>jena-core</artifactId>
      <version>${jena.version}</version>
      <classifier>tests</classifier>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.vintage</groupId>
      <artifactId>junit-vintage-engine</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Only the example evaluator (in src/test) uses shiro. -->
    <dependency>
      <groupId>org.apache.shiro</groupId>
      <artifactId>shiro-core</artifactId>
      <version>2.0.5</version>
      <classifier>jakarta</classifier>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.logging.log4j</groupId>
      <artifactId>log4j-slf4j2-impl</artifactId>
      <version>${log4j-api.version}</version>
      <scope>test</scope>
    </dependency>

  </dependencies>
</project>
